import styled from "styled-components";

export const CurrentPlayerDiv = styled.div`

 
        .turn {
            font-size: 3rem;
        }
        .winner {
            font-size: 5rem;
        }
`