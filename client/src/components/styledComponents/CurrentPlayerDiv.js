import styled from "styled-components";

export const CurrentPlayerDiv = styled.div`

    img {
        width: 100%;
    }
        .turn {
            font-size: 2.5rem;
        }
        .winner {
            font-size: 3rem;
        }
`