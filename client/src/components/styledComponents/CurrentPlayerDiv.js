import styled from "styled-components";

export const CurrentPlayerDiv = styled.div`

 
        .turn {
            font-size: 2.5rem;
        }
        .winner {
            font-size: 3rem;
        }
`